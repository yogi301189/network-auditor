# .gitignore for network-auditor
# ================================

# Terraform — never commit these
infra/.terraform/
infra/.terraform.lock.hcl
infra/terraform.tfvars        # contains your Slack webhook URL — keep it local
infra/terraform.tfstate
infra/terraform.tfstate.backup
infra/lambda/handler.zip      # generated by Terraform, not source code

# Python
__pycache__/
*.pyc
.env

# Reports — these are committed intentionally by the CI pipeline,
# but you don't need to track them locally
# reports/